<div data-post="{{post}}" class="{{class}} tmpl-article giant">

    {{{article_begin}}}

    {{#image}}
    <img src="{{image.url}}" alt="" />
    {{/image}}

    <div class="tmpl-giant-content">
        {{{html_title}}}
        {{html_content}}}
    </div>

</div>

{{include parts/footer.tmpl}}
